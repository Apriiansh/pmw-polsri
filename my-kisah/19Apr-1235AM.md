# Kisah Perjalanan PMW Polsri - 19 April 2026

## 📝 Detail Aktivitas
Pengecekan dan audit halaman publik (Beranda, Tentang, Tahapan, Galeri, Pengumuman) untuk memastikan kualitas UI/UX "Senior Principal" dan konsistensi sistem.

## 🚀 Perubahan yang Dilakukan
1.  **Fix Registration Links:** Mengubah tautan `daftar` menjadi `register` di seluruh halaman publik agar sesuai dengan route yang didefinisikan di `Config/Routes.php` dan default Shield.
2.  **SEO Optimization:** 
    - Implementasi Meta Tags dinamis (Title, Description, Keywords) di `PublicPages` dan `Home` controller.
    - Penambahan Open Graph (OG) tags, Twitter Cards, dan Canonical URLs di `layouts/public.php`.
    - Pembuatan dynamic `Sitemap.xml` via `Sitemap` controller.
    - Konfigurasi `robots.txt` untuk optimasi crawling dan pembatasan akses bot ke area admin/internal.
3.  **Fix Notification Module:**
    - Memperbaiki bug "Mark as Read" dan "Mark All as Read" yang tidak berfungsi karena error pada JavaScript selector.
    - Menambahkan class `notification-row` untuk presisi selector DOM.
    - Menambahkan header CSRF pada request AJAX untuk keamanan.
    - Sinkronisasi return status pada `NotificationsController`.

## 🔍 Temuan Audit
-   Halaman 404 masih menggunakan template default CI4 (perlu modernisasi).
-   Filter pada Galeri dan Pengumuman masih bersifat statis (UI-only).
-   Seluruh aset gambar masih menggunakan placeholder Unsplash (perlu diganti dengan aset riil nantinya).

## 📌 Status Terkini
Halaman publik telah dioptimasi untuk SEO dan navigasi. Modul notifikasi kembali berfungsi normal secara realtime via AJAX. Siap untuk tahap integrasi data dinamis lebih lanjut.
