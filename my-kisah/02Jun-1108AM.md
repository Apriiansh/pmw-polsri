# 02 Jun 2026 — 11:08 AM

## Slider Persentase Nilai Pitching

User request: ubah input `persentase_nilai` (number input) jadi slider yang interaktif + cantik.

### Iterasi 1: 4-tier slider (Tidak/Dipertimbangkan/Direkomendasikan/Sangat)
- File: `app/Views/admin/pitching/validation_detail.php`, `app/Views/penilai/pitching/validation_detail.php`
- Native range slider + floating bubble + 4 tier quick-snap cards
- Tick marks di 0, 55, 70, 85, 100
- `accent-color` dinamis: rose/amber/sky/emerald

### Iterasi 2: Simplify ke 2-tier (≥80 LOLOS, <80 TIDAK LOLOS)
User feedback: "ganti saja jadi <80% tidak lolos >=80% lolos"

#### File diubah (4 total):
- `app/Views/admin/pitching/validation_detail.php` — slider block disederhanakan
- `app/Views/penilai/pitching/validation_detail.php` — sama untuk konsistensi
- `app/Views/admin/pitching/validation.php` — list view coloring 2-tier + icon
- `app/Views/penilai/pitching/validation.php` — sama

#### Perubahan slider:
- `accent` getter: rose jika <80, emerald jika ≥80
- `label` getter: 2 opsi (LOLOS / TIDAK LOLOS), range "≥ 80" / "< 80"
- `activeTier` getter: 0 atau 1
- Tick marks: 0, 80, 100 (hijau di 80)
- 2 tier cards (sebelumnya 4): "Tidak Lolos < 80%" / "Lolos ≥ 80%"
- Snap values: 40 (tidak lolos), 90 (lolos)

#### Perubahan list view:
- Coloring 2-tier: emerald (≥80) / rose (<80)
- Tambahan icon: `fa-trophy` (lolos) / `fa-circle-xmark` (tidak lolos)

#### Verified:
- `php -l` semua 4 file: No syntax errors
- Form submission tetap: hidden input `name="persentase_nilai"` kirim nilai
- Batas kelulusan eksplisit ditampilkan di label helper

---

## Iterasi 3: Catatan Validasi Wajib Diisi

User request: "Field Catatan di validasi itu buat biar important, gaboleh null, tambah validasi aja"

### File diubah (4):
- `app/Controllers/Admin/PitchingDeskController.php` — `validateAction()` tambah `$this->validateData(['catatan' => 'required|min_length[5]|max_length[1000]'])`
- `app/Controllers/Penilai/PitchingDeskController.php` — sama untuk konsistensi role
- `app/Views/admin/pitching/validation_detail.php` — textarea `required minlength=5`, label "Wajib" badge (rose), error banner
- `app/Views/penilai/pitching/validation_detail.php` — sama

### Implementasi:
- **Client-side**: `<textarea required minlength="5" maxlength="1000">` → browser prevent submit jika kosong
- **Server-side**: `validateData()` dengan rule `required|min_length[5]|max_length[1000]` → jika gagal, `redirect()->back()->withInput()->with('error', ...)`
- **UI feedback**:
  - Red asterisk `*` setelah label
  - Badge "Wajib" kecil (rose-50) di sebelah label
  - Helper text ganti jadi rose-tinted
  - Banner error di atas form saat validation fail (rose-50, dismissable)
- **Logic fix**: `$catatan ?: null` diganti `$catatan` (karena sudah divalidasi required)

### Verified:
- `php -l` 4 file: No syntax errors
- Form harus di-reject jika catatan kosong atau < 5 char
- Pesan error: "Catatan validasi wajib diisi (minimal 5 karakter) agar mahasiswa mendapat umpan balik yang jelas."

